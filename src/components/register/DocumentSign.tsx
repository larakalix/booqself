import { useState } from "react";

export const DocumentSign = () => {
    const [docusign, setDocusign] = useState(null);

    return (
        <div>
            <button onClick={() => {}}>Open DocuSign</button>
        </div>
    );
};
